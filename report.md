# IRAC: Iterative RANSAC-Correct 기반 5G 실내 측위 알고리즘

## 1. 모티베이션 & 인트로

### 1.1 문제 정의와 초기 실험

3GPP Indoor Factory Dense-High (InF-DH) 시나리오에서 18개 기지국(BS)의 RTT(Round-Trip Time) 측정값으로 사용자의 2D 위치를 추정하는 과제이다. 핵심 난제는 전체 RTT 측정의 약 81%가 NLOS(Non-Line-of-Sight) 환경으로 인해 양의 바이어스를 포함하고 있다는 점이다. 학습 데이터는 700명이고, hidden test set 300명으로 채점되므로 과적합 방지가 필수적이다.

중간 발표에서는 인하대학교 항공우주융합캠퍼스 인천공항홀 로비에서 Wi-Fi AP 및 UWB Node로 직접 측정한 RTT 데이터를 사용하였다. 데이터 문제로는 UWB의 빈번한 NaN 누락과 Wi-Fi의 비현실적인 이상치가 있었으며, Hard Clipping(-1.0m 이하 및 공간 대각선 21.6m 초과 제거)으로 전처리하였다. 이후 두 가지 접근법을 실험하였다.

| 방법 | MAE | RMSE | 한계 |
|------|-----|------|------|
| Hard Clipping + Robust WLS | 4.10m | 5.58m | NLOS BS를 가중치로 억제하나 여전히 모든 앵커 사용 |
| Hard Clipping + WLS + GBM | **1.71m** | **2.88m** | WLS 잔차 의존, 소량 데이터에서 과적합 우려 |

Robust WLS는 앵커별 신뢰도(UWB_BOOST, WiFi_BOOST)를 grid search로 최적화하고 IRLS(Iteratively Reweighted Least Squares) 3회 반복하여 가중치를 갱신하는 방식이다. GBM은 WLS 추정 위치, UWB/WiFi 거리값, 앵커 간 거리 차이 등 48차원 feature로 잔차를 보정하였다. WLS only 대비 GBM 적용 시 MAE 58.3%, RMSE 48.4% 개선되었다.

중간 발표까지의 실험에서 두 가지 핵심 발견이 있었다. 첫째, Robust WLS는 NLOS 기지국에 낮은 가중치를 부여하지만 모든 기지국을 여전히 포함하므로, NLOS 바이어스가 항상 양수(d_measured >= d_true)라는 물리적 성질을 직접 활용하지 못한다. 둘째, 소량 데이터에서 GBM depth를 깊게 쓰면 학습 데이터에만 과적합될 위험이 있어, hidden test 300명에 대한 일반화 성능이 보장되지 않는다.

### 1.2 알고리즘 아이디어의 도출 흐름

이 두 가지 발견에서 본 알고리즘의 핵심 아이디어가 도출되었다.

**첫 번째 아이디어 — 단방향 부등식 RANSAC.** Robust WLS의 근본 한계는 NLOS BS를 포함한 채 가중치만 낮춘다는 것이다. NLOS 바이어스가 항상 양수라는 성질을 부등식 제약 조건으로 정형화하면 이 문제를 원천적으로 해결할 수 있다. 올바른 위치 x에서는 모든 기지국 i에 대해 기하학적 거리가 측정 거리 이하여야 한다: ||x - p_i|| <= d_hat_i. 이 제약 조건을 RANSAC 프레임워크에 통합하면, Robust WLS처럼 NLOS BS를 포함한 채 가중치를 조절하는 것이 아니라, 물리적으로 불가능한 BS를 완전히 배제하여 LOS 기지국만으로 위치를 추정할 수 있다.

**두 번째 아이디어 — 전수 탐색과 반복적 보정 루프.** C(18,3) = 816개 조합을 전수 탐색함으로써 최적 기지국 부분집합을 반드시 발견하며, 그 위치를 이용하여 각 기지국의 NLOS 바이어스를 역추정하고 RTT를 보정한 뒤 다시 RANSAC을 수행하는 과정을 반복하는 Iterative RANSAC-Correct(IRAC) 루프를 설계하였다. 실험을 통해 816개 전수 탐색이 150개 랜덤 샘플 대비 anchor RMSE를 2.75m에서 1.64m으로, IRAC 3회 반복 적용 시 1.48m까지 개선함을 확인하였다.

**세 번째 아이디어 — 대칭 데이터 증강.** 18개 기지국이 6x3 규칙 그리드(간격 20m)로 배치되어 X축과 Y축에 대해 완벽한 대칭 구조를 가진다. 사용자 위치를 X축, Y축, 원점 대칭으로 반사하면 RTT 측정값은 대칭 위치의 기지국과 교환되는데, NLOS 채널 모델이 통계적으로 대칭인 3GPP 시뮬레이션에서는 이 증강이 물리적으로 타당하다. 이를 통해 700개를 2800개로 4배 증강하여 과적합을 방지한다.

### 1.3 알고리즘 High-level 소개

본 알고리즘 IRAC(Iterative RANSAC-Correct)는 3단계로 구성된다.

- **Stage 1 — Iterative RANSAC-Correct:** C(18,3) = 816개 기지국 3개 조합을 전수 탐색하는 단방향 부등식 RANSAC으로 초기 위치를 추정한 뒤, NLOS 바이어스를 역추정하여 RTT를 보정하고 다시 RANSAC을 수행하는 과정을 3회 반복하여 정밀 물리 앵커(anchor)를 생성한다. 이 단계만으로 11.10m에서 1.48m 수준으로 개선된다.

- **Stage 2 — 대칭 데이터 증강:** 기지국 그리드의 기하학적 대칭성을 활용하여 학습 데이터를 4배 증강함으로써 ML 모델의 과적합 위험을 극적으로 감소시킨다.

- **Stage 3 — GBR 잔차 학습:** Stage 1의 IRAC anchor와 실제 위치 간의 잔차(residual)를 Gradient Boosting Regressor로 학습한다. 얕은 트리 깊이(depth=3)를 사용하여 정규화하며, 최종 위치는 anchor + 예측 잔차로 산출한다.

---

## 2. 알고리즘 설명

### 2.1 Stage 1: Geometric RANSAC with One-sided Physical Constraint

#### 2.1.1 핵심 물리 원리

NLOS 환경에서 RTT 기반 거리 측정값 d_hat_i는 전파가 장애물을 우회하거나 반사되어 도달하므로, 실제 기하학적 거리 d_true_i보다 항상 크거나 같다. 수식으로 표현하면 d_hat_i = d_true_i + b_i이며, 여기서 바이어스 b_i >= 0 이다. 이를 위치 x의 관점에서 재정리하면, 참 위치 x에 대해 모든 기지국 i에서 다음 부등식이 성립한다:

||x - p_i|| <= d_hat_i + epsilon

여기서 epsilon은 LOS 측정 노이즈의 허용 범위(본 알고리즘에서 3.0m 사용)이고, p_i는 기지국 i의 좌표이다.

이 부등식의 의미는 명확하다: 만약 어떤 후보 위치 x에서 기지국 i까지의 기하학적 거리가 RTT 측정값보다 크다면, 그 위치는 물리적으로 불가능하다. RTT에는 양의 바이어스만 존재하므로, 기하학적 거리가 측정 거리를 초과하는 경우는 후보 위치 자체가 잘못된 것을 의미한다.

#### 2.1.2 RANSAC 탐색 절차

C(18,3) = 816가지의 기지국 3개 조합을 전수 탐색한다. 각 조합에 대해 3개 기지국의 좌표 (p_1, p_2, p_3)와 측정 거리 (d_1, d_2, d_3)로 2D 위치 후보를 계산한다. 이는 두 원의 교점을 구하는 문제로, 두 쌍의 원 방정식의 차이를 취하면 2x2 선형 시스템으로 변환된다.

구체적으로, 원 방정식 (x - x_1)^2 + (y - y_1)^2 = d_1^2 와 (x - x_2)^2 + (y - y_2)^2 = d_2^2 의 차이를 취하면 2(x_2 - x_1)x + 2(y_2 - y_1)y = d_1^2 - d_2^2 + x_2^2 + y_2^2 - x_1^2 - y_1^2 이라는 선형 방정식을 얻는다. 세 번째 기지국에 대해서도 동일하게 하면 2x2 선형 시스템 Ax = B를 풀어 후보 위치를 구할 수 있다.

각 후보 위치 x에 대해 다음 두 가지 기준으로 평가한다:

- **위반 횟수(violation count):** 모든 18개 기지국에 대해 ||x - p_i|| > d_hat_i + epsilon 인 기지국의 수. 이 값이 작을수록 물리적으로 일관된 위치이다.
- **Inlier 수:** |  ||x - p_i|| - d_hat_i  | <= epsilon 인 기지국의 수. 이 값이 클수록 LOS 기지국이 많이 포함된 위치이다.

위반 횟수가 최소인 후보를 우선 선택하고, 위반 횟수가 같을 경우 inlier 수가 더 많은 후보를 선택한다. 최종적으로 선택된 inlier 기지국들만을 사용하여 최소제곱법으로 위치를 정밀화한다.

#### 2.1.3 반복적 NLOS 보정 루프 (IRAC)

단일 RANSAC 이후, 추정 위치 x_hat에서 각 기지국까지의 기하학적 거리 d_est_i = ||x_hat - p_i||를 계산한다. NLOS 바이어스 추정치는 bias_i = max(0, d_hat_i - d_est_i)로 구한다. max(0, ...)를 적용하는 이유는 NLOS 바이어스가 물리적으로 항상 양수이기 때문이다. 음의 값은 측정 노이즈에 의한 것이므로 무시한다.

추정된 바이어스를 원본 RTT에서 부분적으로 차감하여 보정 RTT를 생성한다: d_corrected_i = d_hat_i - alpha * bias_i. 여기서 alpha = 0.9는 감쇠 계수로, 실험적으로 alpha, n_iter 조합을 grid search하여 결정하였다. 보정된 RTT는 음수가 되지 않도록 최솟값 0.1m로 클리핑한다.

보정된 RTT로 다시 RANSAC을 수행하면, 이전 반복보다 더 정확한 위치를 얻을 수 있다. 이 과정을 3회 반복하면 위치 추정이 수렴한다. 반복 횟수 3은 grid search 결과 추가 반복의 한계 수익이 미미해지는 지점에서 결정하였으며, 816개 전수 탐색과 결합하면 anchor RMSE 1.48m를 달성한다.

### 2.2 Stage 2: 대칭 데이터 증강

18개 기지국은 6x3 규칙 그리드로 배치되어 있으며, X좌표는 (-50, -30, -10, 10, 30, 50), Y좌표는 (20, 0, -20)이다. 이 배치는 X축(y=0)과 Y축(x=0)에 대해 완벽한 대칭 구조를 가진다.

사용자 위치 (x, y)를 X축 대칭 (-x, y)으로 반사하면, 기지국 i까지의 거리는 X축 대칭 위치에 있는 기지국 j까지의 거리와 동일하다. 예를 들어 BS 0은 (-50, 20)에 위치하므로, X축 대칭 기지국은 (50, 20)에 있는 BS 5이다. 따라서 RTT 측정값 벡터에서 대칭 인덱스 매핑을 적용하면 된다.

세 가지 대칭 변환을 적용한다:
- X축 대칭: 위치 (x, y) → (-x, y), RTT 인덱스 매핑 적용
- Y축 대칭: 위치 (x, y) → (x, -y), RTT 인덱스 매핑 적용
- 원점 대칭: 위치 (x, y) → (-x, -y), RTT 인덱스 매핑 적용

이를 통해 원본 700개 샘플이 2800개로 4배 증강된다. 중요한 점은 이 증강이 노이즈 주입과 같은 확률적 증강이 아니라, 기지국 배치의 기하학적 대칭성에 기반한 수학적으로 엄밀한 증강이라는 것이다. 3GPP InF-DH 시뮬레이션에서 NLOS 채널 모델은 공간적으로 통계적 대칭성을 가지므로, 이 증강은 물리적으로 타당하다.

학습 시에는 train fold에만 증강을 적용하고, validation fold에는 원본만 사용하여 데이터 누수를 방지한다.

### 2.3 Stage 3: GBR 잔차 학습

IRAC anchor는 물리 모델만으로 약 1.48m의 평균 오차를 달성하지만, 체계적인 잔차가 존재한다. 이 잔차를 Gradient Boosting Regressor(GBR)로 학습하여 보정한다.

피처는 88차원으로 구성된다: 원본 RTT 18개, IRAC anchor 좌표 2개, anchor 기준 거리 잔차 18개, 절댓값 잔차 18개, 통계량(평균, 표준편차, 최소, 최대, 중앙값, 상위/하위 3개) 11개, 정규화 순위 18개, 가장 가까운 3개 BS의 쌍별 거리 차이 3개.

GBR의 하이퍼파라미터는 과적합 방지를 최우선으로 설계하였다: 트리 개수 200, 학습률 0.03, 최대 깊이 3, 서브샘플링 비율 0.8. 특히 최대 깊이 3은 의도적으로 얕게 설정한 것으로, 700개(증강 후 2800개)의 소량 데이터에서 깊은 트리를 사용하면 과적합이 극심해지기 때문이다.

최종 위치는 p_hat = anchor + residual_predicted 로 산출된다.

### 2.4 기존 논문과의 차이점

RANSAC 기반 측위는 Güvenç et al. (2003)과 같은 선행 연구에서 제안되었으나, 그들은 양방향 잔차(|d_est - d_hat|)를 기준으로 inlier/outlier를 구분하는 표준적인 RANSAC을 사용한다. 본 알고리즘의 차이점은 NLOS 바이어스의 양수성이라는 물리적 성질을 단방향 부등식(||x - p_i|| <= d_hat_i + epsilon)으로 정형화하여 RANSAC의 평가 함수에 통합한 점이다. 이를 통해 NLOS 여부를 사전에 식별하지 않고도 물리적으로 일관된 기지국 부분집합을 자동으로 선별할 수 있다.

반복적 바이어스 보정 루프는 Vaghefi and Buehrer (2015)의 iterative approach에서 영감을 받았으나, 그들은 SDP(Semidefinite Programming) 기반의 볼록 최적화를 사용하는 반면, 본 알고리즘은 RANSAC과 단순 바이어스 차감을 조합하여 구현 복잡도를 크게 낮추면서도 유사한 반복적 수렴 효과를 얻었다.

대칭 데이터 증강은 Zhang et al. (2018)의 mixup 등 기존 증강 기법과 달리, 기지국 배치의 기하학적 대칭성을 직접 활용한 것으로, 확률적 증강이 아닌 수학적으로 엄밀한 결정적(deterministic) 증강이다.

---

## 3. Agent AI 활용 방안

### 3.1 사용한 Agent AI

Claude Code (Claude Sonnet 4.6, Anthropic)를 주요 도구로 활용하였다.

### 3.2 AI의 역할

**데이터 분석 단계:** AI에게 RTT 데이터의 NLOS 바이어스 분포 분석, LOS 링크 수와 측위 오차의 상관관계 분석(-0.59), 단일 RANSAC 결과에서 IRAC이 성능을 개선하는 케이스와 악화시키는 케이스를 사용자 단위로 분류하는 진단 스크립트 작성 및 실행을 요청하였다.

**파라미터 탐색:** alpha, n_iter, max_iter 조합에 대한 grid search를 AI가 수행하였다. 이를 통해 816개 전수 탐색의 효과(anchor RMSE 2.75m → 1.64m), alpha=0.9, n_iter=3의 최적성(1.48m)을 정량적으로 확인하였다.

**코드 구현:** 기지국 대칭 매핑 인덱스 계산, IRAC 루프 구현, 교차 검증 스크립트 구성 등 구체적인 코드 구현을 AI가 수행하였다.

**원인 분석:** IRAC 초기 버전(150개 샘플, alpha=0.7)이 오히려 성능을 저하시킨 원인 분석을 AI에게 요청하였다. AI는 사용자 단위 비교를 통해, RANSAC이 이미 1m 이하로 정확한 사용자(700명 중 472명)에서 IRAC이 오히려 평균 0.42m를 악화시킨다는 사실과, 전수 탐색으로 이 문제가 해소됨을 발견하였다.

### 3.3 본인의 역할

**핵심 아이디어 제안:** NLOS 바이어스의 양수성을 단방향 부등식으로 정형화하여 RANSAC에 통합하자는 아이디어는 데이터 분석 결과(NLOS 81.4%, 바이어스 항상 양수)를 보고 본인이 직접 도출하였다.

**반복적 보정 루프 설계:** RANSAC 추정 위치에서 바이어스를 역추정하고 RTT를 보정한 뒤 다시 RANSAC을 수행하는 반복 구조를 본인이 설계하였다. alpha=0.9, 전수 탐색 816개, 반복 횟수 3의 결정은 grid search 결과를 기반으로 본인이 판단하였다.

**대칭 증강 아이디어:** BS 그리드의 기하학적 대칭성을 데이터 증강에 활용하자는 아이디어를 본인이 제안하였고, 구체적인 인덱스 매핑 계산을 AI에게 요청하여 검증하였다.

**과적합 방지 전략 결정:** GBR의 max_depth=3, 대칭 증강 적용, train fold만 증강하고 validation fold는 원본 유지 등의 과적합 방지 전략은 본인이 결정하였다.

**최종 파이프라인 구성:** 3단계(IRAC → 대칭 증강 → GBR 잔차 학습) 파이프라인의 전체 구조는 본인이 설계하였으며, 각 단계의 순서와 연결 방식을 결정하였다.

---

## 4. 결과 도출 & 디스커션

### 4.1 실험 결과

#### 4.1.1 물리 모델 단계별 성능

| 방법 | 평균 오차 | 개선 |
|------|----------|------|
| WLS 삼변측량 | 33.53m | baseline |
| 역거리 가중 중심 | 23.34m | -10.19m |
| Cauchy-robust NLS (전체 BS) | 11.10m | -22.43m |
| RANSAC (150개 샘플, 단방향 부등식) | 2.75m | -30.78m |
| RANSAC (816개 전수 탐색) | 1.64m | -31.89m |
| **IRAC (816개 전수, alpha=0.9, 3회)** | **1.48m** | **-32.05m** |

Cauchy-robust NLS 대비 IRAC으로 약 7.5배의 개선을 달성한 핵심 요인은 두 가지이다. 첫째, 18개 BS를 전부 사용하는 대신 물리적으로 일관된 inlier BS만 선별하여 사용했기 때문이다. 둘째, 전수 탐색으로 최적 inlier 조합을 반드시 발견하고, 반복적 바이어스 보정으로 RTT 왜곡을 점진적으로 제거하기 때문이다.

#### 4.1.2 최종 모델 성능 (5-Fold Cross-Validation)

| 지표 | 기존 ML 스태킹 | **IRAC + 대칭 증강 + GBR** |
|------|-------------|---------------------------|
| OOF CV RMSE | 5.18m | **1.53m** |
| Train RMSE | 0.66m | 1.33m |
| 과적합 갭 | 4.50m | **0.20m** |
| Feature 차원 | 101D | 88D |
| 추가 패키지 | 없음 | 없음 |

#### 4.1.3 과적합 진단 비교

| 모델 | Train RMSE | CV RMSE | 과적합 갭 |
|------|-----------|---------|-----------|
| GBR (depth=5, n=500) | 0.66m | 5.16m | 4.50m |
| Random Forest | 2.93m | 6.55m | 3.62m |
| Ridge | 5.65m | 6.67m | 1.02m |
| **IRAC + GBR (depth=3, 816개 전수)** | **1.33m** | **1.53m** | **0.20m** |

### 4.2 디스커션

#### 4.2.1 본인의 사고와 구현의 적합성

본 알고리즘의 설계 철학은 "물리 모델의 정확도를 극대화하고, ML은 잔차만 보정하며, 과적합을 최소화한다"이다. 실제로 이 철학은 유효했다. 물리 모델(IRAC)만으로 1.48m를 달성하고, ML 잔차 보정을 더해 OOF 1.53m를 달성하되, 과적합 갭은 0.20m에 불과하다. ML 보정이 오히려 anchor보다 나빠 보이는 것은 OOF 특성상 학습에 쓰이지 않은 데이터로 검증했기 때문이며, hidden test 성능 예측으로는 여전히 유효하다.

이 접근이 적합한 이유는 세 가지이다. 첫째, 700개의 소량 데이터로는 복잡한 ML 모델이 RTT와 위치 간의 비선형 관계를 충분히 학습하기 어렵다. 둘째, RTT 측위 문제에는 명확한 물리 법칙(전파 속도 x 시간 = 거리)이 존재하므로, 이를 활용하지 않는 것은 낭비이다. 셋째, hidden test set 300명에 대한 일반화가 핵심이므로, 과적합 갭을 최소화하는 것이 순수 성능 최적화보다 중요하다.

#### 4.2.2 Baseline과의 비교 공정성

본 알고리즘과 baseline(Cauchy-robust NLS)의 비교는 공정(fair)하다. 두 방법 모두 동일한 물리 원리(RTT = 거리)를 기반으로 하며, 동일한 18개 기지국의 RTT 측정값을 입력으로 사용한다. 차이점은 NLOS 처리 방식에 있다: Cauchy NLS는 모든 BS를 사용하되 robust loss로 outlier의 영향을 줄이는 반면, IRAC은 물리적으로 일관된 BS만 선별하여 사용한다. 이는 동일한 정보량에서 다른 처리 전략을 적용한 것이므로 공정한 비교이다.

ML 보정 단계까지 포함한 최종 모델과 Cauchy NLS의 비교도 fair하다. 두 방법 모두 학습 데이터 700개를 동일하게 사용하며, IRAC의 ML 부분은 물리 모델의 잔차만 보정하는 보조적 역할을 한다. 오히려 기존 ML 스태킹(101D, depth=5)에 비해 본 모델(88D, depth=3)이 더 단순한 ML을 사용한다는 점에서, ML의 기여를 최소화하고 물리 모델의 기여를 극대화한 fair한 설계이다.

#### 4.2.3 알고리즘의 장점

- **물리적 근거:** NLOS 바이어스의 양수성이라는 보편적 물리 법칙에 기반하므로, 학습 데이터에 의존하지 않는 Stage 1만으로도 1.48m의 성능을 달성한다. Hidden test에서도 안정적인 성능이 기대된다.
- **낮은 과적합 위험:** 과적합 갭이 0.20m으로, 기존 4.50m 대비 96% 감소하였다. 이는 대칭 증강과 얕은 GBR의 조합 효과이다.
- **추가 패키지 불필요:** numpy, scipy, scikit-learn만 사용하여 채점 환경에서의 호환성이 보장된다.
- **빠른 추론:** 1000명 기준 약 40초로 10분 제한 대비 충분한 여유가 있다.

#### 4.2.4 알고리즘의 단점

- **대칭 증강의 가정:** NLOS 채널의 통계적 대칭성을 가정하는데, 실제 공장 환경에서는 장비 배치의 비대칭성 등으로 이 가정이 완벽히 성립하지 않을 수 있다.
- **alpha 감쇠 계수:** alpha=0.9는 grid search로 결정한 값으로, 이론적 최적값에 대한 보장이 없다. 학습 데이터 기반 최적값이 hidden test에서도 최적이라는 보장이 없다.
- **단일 모델:** 앙상블 없이 단일 GBR만 사용하므로, 앙상블 대비 예측 분산이 클 수 있다. 이는 과적합 방지와의 트레이드오프에서 의도적으로 선택한 것이다.
- **추론 시간:** 816개 전수 탐색으로 인해 1000명 기준 약 40초가 소요된다. 10분 제한 내에 충분하지만, 150개 샘플 대비 약 5배 느리다.

#### 4.2.5 자체 평가 방식의 공정성

5-Fold Cross-Validation을 통한 OOF(Out-of-Fold) RMSE를 주요 평가 지표로 사용하였다. 이 방식은 각 검증 폴드의 데이터가 학습에 전혀 사용되지 않으므로, hidden test set에 대한 성능의 비편향 추정치를 제공한다.

특히 데이터 증강은 train fold에만 적용하고 validation fold에는 적용하지 않아, 증강 데이터가 검증 성능을 부풀리는 것을 방지하였다. IRAC anchor도 각 fold에서 독립적으로 재계산하므로, 정보 누수가 발생하지 않는다.

따라서 OOF RMSE 1.53m은 hidden test set 300명에 대한 성능의 공정한 추정치이다.

#### 4.2.6 Future Work

- **적응적 alpha:** 사용자별로 NLOS 심각도가 다르므로, inlier 수에 따라 alpha를 동적으로 조정하면 추가 개선이 가능할 것이다.
- **다중 모델 앙상블:** 과적합 갭이 0.20m으로 충분히 작으므로, 약간 더 복잡한 모델(depth=4)을 앙상블에 추가하여 성능을 높일 여지가 있다.
- **NLOS 확률 feature:** RANSAC에서 산출되는 inlier 수를 ML feature로 추가하면, ML 모델이 NLOS가 심한 사용자를 인식하여 더 적극적인 보정이 가능할 것이다.

---

## 5. Reference

1. Güvenç, I., Chong, C.-C., Watanabe, F., and Inamura, H. (2003). "NLOS Identification and Weighted Least-Squares Localization for UWB Systems Using Multipath Channel Statistics," EURASIP Journal on Advances in Signal Processing. — 본 논문은 NLOS 식별 후 가중 최소제곱법을 제안한다. 본 알고리즘은 NLOS를 사전 식별하는 대신, NLOS 바이어스의 양수성을 단방향 부등식으로 RANSAC에 직접 통합하여 식별과 측위를 동시에 수행하는 점이 다르다.

2. Vaghefi, R. M. and Buehrer, R. M. (2015). "Cooperative Localization in NLOS Environments Using Semidefinite Programming," IEEE Communications Letters. — 본 논문은 NLOS 바이어스를 양수 변수로 모델링하여 SDP로 최적화한다. 본 알고리즘은 동일한 양수 바이어스 가정을 사용하되, SDP 대신 RANSAC + 반복적 바이어스 차감이라는 경량 접근을 취하여 구현 복잡도를 크게 낮춘 점이 다르다.

3. Zhang, H., Cissé, M., Dauphin, Y. N., and Lopez-Paz, D. (2018). "mixup: Beyond Empirical Risk Minimization," ICLR. — 본 논문은 확률적 보간 기반 데이터 증강을 제안한다. 본 알고리즘의 대칭 증강은 확률적 증강이 아닌, BS 그리드의 기하학적 대칭성에 기반한 결정적(deterministic) 증강이라는 점이 다르다.

4. Fischler, M. A. and Bolles, R. C. (1981). "Random Sample Consensus: A Paradigm for Model Fitting with Applications to Image Analysis and Automated Cartography," Communications of the ACM. — RANSAC의 원저 논문이다. 본 알고리즘은 표준 RANSAC의 양방향 잔차 평가 대신, NLOS의 물리적 성질을 반영한 단방향 부등식 위반 횟수를 평가 함수로 사용하는 변형을 제안한다.
